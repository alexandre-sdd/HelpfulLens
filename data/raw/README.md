# Raw Source Files

Drop the untouched Yelp Academic Dataset JSON (or `.json.gz`) files here. Name
them exactly as downloaded (e.g., `yelp_academic_dataset_review.json`).

The ingestion step detects both subfolders and flat files, so you do not need to
reorganize the official dump.
