name: helpfullens
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - pandas
      - numpy
      - scikit-learn
      - matplotlib
      - seaborn
      - jupyter
      - pyyaml
      - pyarrow
      - nbstripout
      - vaderSentiment
      - transformers
      - torch
      - tqdm
